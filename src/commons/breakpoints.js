
const size = {
  sm: '425px',
  md: '768px',
  lg: '1200px'
}

const device = {
  phone: `max-width: ${size.sm}`,
  tablet: `max-width: ${size.md}`,
  laptop: `max-width: ${size.lg}`
}
export default {size, device}